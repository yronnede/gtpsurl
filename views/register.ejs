<!DOCTYPE html>
<html>
<head>
    <title>Register for GTPS Account</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #000000;
            font-family: Arial, sans-serif;
        }
        .container {
            background-color: #111111;
            padding: 2rem;
            border-radius: 12px;
            width: 100%;
            max-width: 380px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }
        .header {
            text-align: center;
            margin-bottom: 2rem;
        }
        .header .title {
            font-size: 1.8rem;
            font-weight: bold;
            color: #FFE600;
        }
        .form-group {
            margin-bottom: 1.2rem;
        }
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: #808080;
            font-size: 0.9rem;
        }
        .form-group input {
            width: 100%;
            padding: 0.8rem;
            border: none;
            border-radius: 6px;
            background-color: #1E1E1E;
            color: #ffffff;
            font-size: 0.9rem;
            box-sizing: border-box;
        }
        .form-group input::placeholder {
            color: #4A4A4A;
        }
        .register-button {
            width: 100%;
            padding: 0.9rem;
            background-color: #FFE600;
            border: none;
            border-radius: 6px;
            color: #000000;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            margin-top: 0.5rem;
        }
        .register-button:hover {
            background-color: #FFD700;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <span class="title">Register for GTPS Account</span>
        </div>
        <form method="POST" action="/player/register/submit">
            <div class="form-group">
                <label for="growid">GTPSID</label>
                <input type="text" id="growid" name="growId" placeholder="Enter your GTPSID" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" placeholder="Enter your password" required>
            </div>
            <button type="submit" class="register-button">Register</button>
        </form>
    </div>
</body>
</html> 